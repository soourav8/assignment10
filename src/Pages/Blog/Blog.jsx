import React, { useContext } from 'react';






const Blog = () => {
    const {user}= useContext(AuthContext)
    return (
        <div>
           
           
        </div>
    );
};

export default Blog;